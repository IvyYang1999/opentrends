{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --filter @opentrends/infra dev",
    "start": "bun run dist/index.mjs",
    "test": "bun test src/trends/__tests__"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^2.0.46",
    "@opentrends/api": "workspace:*",
    "@opentrends/auth": "workspace:*",
    "@opentrends/db": "workspace:*",
    "@opentrends/env": "workspace:*",
    "@orpc/openapi": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/zod": "catalog:",
    "ai": "^6.0.175",
    "better-auth": "catalog:",
    "cheerio": "^1.2.0",
    "defuddle": "^0.19.3",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1",
    "hono": "catalog:",
    "jsdom": "^29.1.1",
    "rss-parser": "^3.13.0",
    "undici": "^7.16.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@opentrends/config": "workspace:*",
    "@types/bun": "catalog:",
    "@types/jsdom": "^28.0.1",
    "tsdown": "^0.21.9",
    "typescript": "catalog:",
    "vite": "^8.0.8"
  }
}
