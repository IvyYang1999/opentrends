// Features that are built but not shown. Flip one to bring it back; the
// routes and code behind it stay in place either way.
export const SHOW_CALENDAR_VIEW = false;
export const SHOW_CONTENT_KINDS = false;
