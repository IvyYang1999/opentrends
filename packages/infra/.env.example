# Required: generate a private value and keep it out of Git.
ALCHEMY_PASSWORD=

# These placeholders are sufficient for local Miniflare development only.
# Cloud deployment requires `bun run cloudflare:login` instead.
CLOUDFLARE_ACCOUNT_ID=local
CLOUDFLARE_API_TOKEN=local-development-token
